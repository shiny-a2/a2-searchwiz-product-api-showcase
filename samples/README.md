# Sample Code Plan

Future sanitized samples may include:

- a generic snapshot row transformer;
- a rebuild-lock guard;
- a response mapper for a public-safe product DTO;
- a request allow/deny boundary example.

Samples must avoid real endpoints, IPs, product data, provider contracts, or access rules.
